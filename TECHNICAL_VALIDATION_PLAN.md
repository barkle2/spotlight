# Spotlight 기술 검증 계획서

## 1. 목적

Spotlight 본 프로그램을 작성하기 전에 Windows 전역 포인터 크기 변경, 투명 오버레이, 전역 마우스 입력 감지 및 화면 녹화 호환성을 작은 Python 프로토타입으로 검증한다.

검증 결과를 바탕으로 실제 구현 방식을 확정하고, 지원하기 어려운 환경이나 동작은 프로젝트 제한 사항에 반영한다.

## 2. 검증 원칙

- 각 검증은 독립적으로 실행할 수 있는 최소한의 Python 스크립트로 수행한다.
- 검증 스크립트는 제품 코드로 간주하지 않으며 구현 방식이 확정되면 필요한 부분만 본 프로그램에 반영한다.
- 사용자 Windows 설정을 변경하는 검증은 실행 전 값을 저장하고 정상 종료 시 복원한다.
- 비정상 종료 가능성이 있는 검증은 복구 방법을 먼저 준비한 후 실행한다.
- 결과는 `TECHNICAL_VALIDATION_RESULTS.md`에 환경, 절차, 결과 및 제한 사항과 함께 기록한다.
- 성공 여부는 육안 확인만으로 판단하지 않고 가능한 경우 읽어 온 설정값이나 로그도 함께 확인한다.

## 3. 검증 환경 기록

각 검증을 수행할 때 다음 환경 정보를 결과 문서에 기록한다.

- Windows 버전 및 빌드
- Python 버전
- PySide6 및 입력 감지 라이브러리 버전
- 모니터 수, 해상도 및 DPI 배율
- 사용 중인 Windows 커서 스타일과 크기
- 관리자 권한 사용 여부
- 화면 녹화 프로그램과 캡처 방식

## 4. 검증 순서

### 4.1 Windows 포인터 크기 변경 및 복원

#### 목표

Windows 접근성 설정에서 제공하는 포인터 크기 1~15를 Spotlight에서 읽고 변경할 수 있는지 확인한다. 프로그램 종료 후에는 실행 전 크기로 안전하게 복원되어야 한다.

#### 검증 항목

- 현재 포인터 크기 읽기
- 설정값 1~15와 Windows 포인터 크기의 대응 관계 확인
- 관리자 권한 없이 크기 변경
- 화살표, 텍스트 선택, 링크, 대기 및 크기 조절 커서에 변경 반영
- 설정 앱에 변경된 크기가 표시되는지 확인
- 프로그램 실행 중 포인터 크기 재변경
- 정상 종료 시 실행 전 크기 복원
- 사용자 지정 커서 테마 및 포인터 스타일 유지
- Windows 10과 Windows 11에서 동일한 방식으로 동작하는지 확인
- 변경된 포인터가 화면 녹화에 실제 크기로 포함되는지 확인

#### 안전 및 복구 조건

- 변경 전에 원래 포인터 크기와 필요한 커서 관련 값을 별도 복구 파일에 기록한다.
- 정상 종료 시 현재 값이 Spotlight가 마지막으로 적용한 값과 같을 때만 원래 값으로 복원한다.
- 실행 중 사용자가 Windows 설정에서 직접 값을 바꾼 경우에는 해당 값을 덮어쓰지 않는다.
- 다음 실행에서 이전 비정상 종료가 감지되면 새 값을 적용하기 전에 복구를 시도한다.
- 레지스트리를 사용해야 한다면 변경 대상과 값의 의미를 결과 문서에 정확히 기록한다.

#### 통과 기준

- 관리자 권한 없이 포인터 크기를 설정할 수 있다.
- 주요 시스템 커서 모양에 동일한 크기 설정이 적용된다.
- 정상 종료 후 원래 크기와 커서 스타일이 복원된다.
- 사용자가 실행 중 직접 변경한 설정을 종료 과정에서 덮어쓰지 않는다.
- 비정상 종료 후 복구할 수 있는 절차가 확인된다.

### 4.2 투명 오버레이

#### 목표

마우스 위치를 따라가는 투명한 강조 원을 다른 프로그램의 입력을 방해하지 않고 표시할 수 있는지 확인한다.

#### 검증 항목

- 투명 배경의 원 표시
- 오버레이를 통한 클릭, 더블 클릭, 드래그 및 휠 입력 통과
- 작업표시줄 버튼 없이 표시
- 다른 일반 창보다 위에 표시
- 빠른 마우스 이동 시 추적 지연과 끊김 확인
- 단일 및 다중 모니터 좌표 처리
- 음수 좌표를 사용하는 모니터 배치
- 모니터별 DPI 배율이 다른 환경
- PowerPoint 전체 화면과 일반 전체 화면 프로그램
- 오버레이 활성화 상태의 CPU 및 메모리 사용량 측정

#### 통과 기준

- 오버레이가 마우스 입력을 가로채지 않는다.
- 모든 모니터에서 강조 원의 중심이 포인터 위치와 일치한다.
- 일반적인 마우스 이동에서 눈에 띄는 지연이나 깜빡임이 없다.
- 작업표시줄에 별도의 창 버튼이 나타나지 않는다.

### 4.3 전역 마우스 입력 감지

#### 목표

Spotlight가 활성 창이 아니어도 왼쪽·오른쪽 클릭과 휠 방향을 감지할 수 있는지 확인한다.

#### 검증 항목

- 왼쪽 클릭과 오른쪽 클릭 구분
- 누름과 뗌 이벤트 구분
- 휠 위·아래 방향과 이동량 감지
- 빠른 연속 클릭과 빠른 휠 입력
- 드래그 중 불필요한 클릭 효과 발생 여부
- 일반 권한 프로그램 위에서 입력 감지
- 관리자 권한 프로그램 위에서 입력 감지
- 입력 훅 사용 중 원래 프로그램의 입력에 영향이 없는지 확인
- 종료 시 입력 훅이 정상적으로 해제되는지 확인

#### 통과 기준

- 다른 프로그램을 사용하는 중에도 필요한 입력을 안정적으로 감지한다.
- 원래 프로그램으로 전달되는 클릭과 휠 입력을 변경하거나 차단하지 않는다.
- 반복 입력에서도 이벤트 누락이나 중복이 허용하기 어려운 수준으로 발생하지 않는다.

### 4.4 클릭 및 휠 애니메이션

#### 목표

전역 입력 이벤트를 오버레이 애니메이션으로 자연스럽게 표현할 수 있는지 확인한다.

#### 검증 항목

- 왼쪽·오른쪽 클릭 색상 구분
- 클릭 시 지정 크기의 불투명한 원 점멸
- 휠 위 입력 시 안쪽에서 바깥쪽으로 이동
- 휠 아래 입력 시 바깥쪽에서 안쪽으로 이동
- 빠른 연속 이벤트의 중첩 또는 재시작 방식 비교
- 애니메이션 도중 마우스가 이동할 때 효과의 기준 위치 확인
- 여러 애니메이션이 겹칠 때 성능 확인

#### 통과 기준

- 클릭과 휠 방향을 시각적으로 명확하게 구분할 수 있다.
- 빠른 입력에서도 애니메이션이 과도하게 쌓이거나 화면을 가리지 않는다.
- 애니메이션 실행이 포인터 추적의 부드러움에 영향을 주지 않는다.

### 4.5 화면 녹화 및 화면 공유 호환성

#### 목표

포인터 크기, 강조 원, 클릭 효과 및 휠 효과가 주요 녹화·공유 방식에 포함되는지 확인한다.

#### 검증 대상

- Windows 캡처 도구
- OBS 디스플레이 캡처
- OBS 창 캡처
- PowerPoint 전체 화면 발표
- 사용 가능한 화상회의 프로그램의 전체 화면 공유
- 사용 가능한 화상회의 프로그램의 특정 창 공유

#### 통과 기준

- 디스플레이 전체를 캡처할 때 Spotlight 효과가 녹화된다.
- 각 캡처 방식에서 지원 여부와 제한 사항이 명확하게 구분된다.
- 지원되지 않는 캡처 방식은 사용자에게 안내할 수 있도록 기록한다.

## 5. 구현 방식 확정 기준

검증이 끝나면 다음 사항을 확정한다.

- Windows 포인터 크기 1~15를 적용하는 방법
- 원래 포인터 설정 저장 및 복구 방법
- 비정상 종료 감지 및 복구 방법
- 투명 오버레이의 창 속성과 렌더링 방식
- 전역 입력 감지 방식과 필요한 라이브러리
- 연속 클릭 및 휠 애니메이션 처리 규칙
- 공식 지원할 화면 녹화 및 화면 공유 방식
- 다중 모니터와 DPI 처리 방식

## 6. 중단 및 대체 기준

- 포인터 크기 변경이 Windows 버전에 따라 불안정하거나 사용자 설정을 안전하게 복원할 수 없다면 시스템 포인터 변경 기능은 대체 방식을 검토한다.
- 투명 오버레이가 특정 캡처 방식에 포함되지 않는 경우 해당 방식은 지원 제한으로 기록하고 디스플레이 캡처를 권장한다.
- 관리자 권한 프로그램의 입력을 일반 권한으로 감지할 수 없다면 프로그램 전체를 관리자 권한으로 실행하기 전에 기능 제한 안내를 우선 검토한다.

