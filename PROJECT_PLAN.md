# Spotlight 프로젝트 계획서

## 1. 프로젝트 개요

- 프로젝트명: Spotlight
- 최종 결과물: Windows용 단일 실행 파일 `spotlight.exe`
- 개발 언어: Python
- 목적: 발표 및 화면 녹화 중 마우스 위치와 클릭·휠 동작을 시각적으로 강조
- 실행 방식: 작업표시줄에는 표시하지 않고 시스템 트레이에서 실행

## 2. 지원 환경

- 대상 운영체제: Windows 10/11
- 화면 환경:
  - 단일 및 다중 모니터
  - 서로 다른 해상도와 배율(DPI)
  - 전체 화면 프레젠테이션 및 녹화 프로그램
- 배포 방식:
  - Python 설치 없이 실행 가능한 `.exe`
  - 가능하면 단일 파일 패키징
  - 사용자 설정은 `%LOCALAPPDATA%\Spotlight\settings.ini`에 INI 형식으로 저장
  - 실제 기본 경로는 Windows의 `%LOCALAPPDATA%` 환경변수를 이용해 결정

## 3. 핵심 기능

### 3.1 마우스 포인터 강조

- 실제 마우스 위치를 실시간 추적
- 포인터 주변에 투명한 강조 원 표시
- 강조 원은 마우스 클릭을 방해하지 않음
- 설정 항목:
  - 원의 지름
  - 색상
  - 투명도
  - 테두리 또는 채움 표시 방식

### 3.2 마우스 포인터 크기

Windows에 설정된 현재 커서 테마를 읽어 각 시스템 커서를 설정된 크기로 복제한 뒤, Spotlight 실행 중에만 임시 시스템 커서로 적용한다.

- 포인터 크기는 Windows 접근성 설정과 동일하게 1~15 단계로 제공
- 크기 1은 32×32픽셀이며 단계마다 16픽셀씩 증가
- 화살표, 텍스트, 링크, 대기, 크기 조절 등 14개 시스템 커서를 함께 변경
- 사용자 커서 테마에 지정된 `.cur` 및 `.ani` 파일을 우선 사용
- 커서 경로가 지정되지 않은 항목은 Windows 시스템 리소스를 사용
- 프로그램 실행 시와 설정 화면에서 포인터 크기를 변경할 때 확대 커서를 적용
- 정상 종료 시 `SystemParametersInfo(SPI_SETCURSORS)`로 Windows에 저장된 커서 구성을 다시 불러와 복원
- 복구 정보를 별도 파일에 기록하고 비정상 종료가 감지되면 다음 실행 시 원래 커서를 먼저 복원
- 실행 중 사용자가 Windows 설정에서 커서를 직접 변경한 경우에는 종료 시 해당 변경을 덮어쓰지 않음

### 3.3 클릭 효과

- 왼쪽 또는 오른쪽 버튼 클릭 감지
- 클릭 순간 지정된 색상의 원이 짧게 점멸
- 설정 항목:
  - 최대 크기
  - 지속 시간
  - 왼쪽·오른쪽 클릭의 색상
- 효과는 완전 불투명으로 표시
- 효과는 입력이 발생한 위치에 고정
- 기본 지속 시간은 180ms

### 3.4 휠 효과

- 휠을 위로 움직일 때: 원 안쪽에서 바깥쪽으로 퍼지는 효과
- 휠을 아래로 움직일 때: 바깥쪽에서 안쪽으로 모이는 효과
- 설정 항목:
  - 효과 색상
  - 시작 및 종료 크기
  - 지속 시간
  - 선 두께
- 효과는 완전 불투명으로 표시
- 효과는 입력이 발생한 위치에 고정
- 기본 지속 시간은 250ms

클릭 및 휠 효과는 최대 3개까지 동시에 표시한다. 3개를 초과하면 가장 오래된 효과부터 제거한다.

### 3.5 시스템 트레이

- 실행 시 작업표시줄 버튼을 만들지 않음
- 오른쪽 아래 알림 영역에 트레이 아이콘 표시
- 트레이 메뉴:
  - 설정
  - 프로그램 종료
- 아이콘 더블 클릭으로 설정 창 열기

## 4. 설정 화면

설정 화면을 다음 영역으로 구분한다.

| 영역 | 주요 옵션 |
| --- | --- |
| 포인터 | 포인터 크기 |
| 강조 원 | 크기, 색상, 투명도, 테두리 두께 |
| 클릭 | 활성화, 색상, 크기, 지속 시간 |
| 휠 | 활성화, 색상, 크기, 속도, 선 두께 |

색상과 크기를 바꾸면 설정 창에서 즉시 미리 볼 수 있도록 정의한다.

### 4.1 설정 파일

- 파일명: `settings.ini`
- 저장 위치: `%LOCALAPPDATA%\Spotlight\settings.ini`
- 문자 인코딩: UTF-8
- 색상 표기: `#RRGGBB`
- 투명도 표기: `0`~`100` 정수
- 포인터 크기: `1`~`15` 단계
- 그 외 크기 단위: 논리 픽셀
- 시간 단위: 밀리초(ms)
- 참·거짓 표기: `true` 또는 `false`
- 알 수 없는 항목은 무시하고 잘못된 값은 기본값으로 대체
- 최초 실행 시 기본값이 담긴 설정 파일을 자동 생성
- 설정 파일은 프로그램 시작 시 한 번만 불러옴
- 프로그램 실행 중 설정 파일을 직접 수정한 경우 재시작 후 반영
- 설정 화면에서 값을 변경하면 설정 파일에 저장하고 실행 중인 프로그램에도 즉시 반영
- 설정 저장 시 임시 파일을 먼저 기록한 후 기존 파일을 교체

설정 파일은 `[pointer]`, `[spotlight]`, `[click]`, `[wheel]` 섹션으로 구성하며 `[general]` 섹션은 사용하지 않는다.

```ini
; Spotlight 사용자 설정
; 색상: #RRGGBB
; 투명도: 0~100
; 포인터 크기: 1~15 단계
; 그 외 크기: 논리 픽셀
; 시간: 밀리초

[pointer]
size=1

[spotlight]
enabled=true
diameter=120
color=#FFFF00
opacity=30
style=fill
border_width=3

[click]
enabled=true
left_color=#FF3B30
right_color=#007AFF
duration_ms=180

[wheel]
enabled=true
color=#34C759
duration_ms=250
line_width=4
```

## 5. 기술 구성안

- GUI 및 트레이: `PySide6`
- 마우스 전역 입력 감지: 검증된 전역 입력 훅 라이브러리 사용 및 종료 시 명시적 훅 해제
- 오버레이:
  - 투명 배경
  - 항상 위에 표시
  - 마우스 입력 통과
  - 작업표시줄 미표시
- 애니메이션: Qt 타이머 및 페인팅 기능
- 설정 저장: UTF-8 INI 파일
- EXE 패키징: `PyInstaller` 검토
- 포인터 크기 변경: Windows `CopyImage` 및 `SetSystemCursor`
- 포인터 복원: Windows `SystemParametersInfo(SPI_SETCURSORS)`

프로그램 구조는 다음과 같이 분리한다.

```text
spotlight/
├─ application       프로그램 시작 및 종료
├─ overlay           강조 원과 애니메이션 표시
├─ input             마우스 이동·클릭·휠 감지
├─ settings          설정 모델 및 저장
├─ settings_ui       설정 화면
├─ tray              시스템 트레이
└─ packaging         아이콘과 EXE 빌드 설정
```

## 6. 비기능 요구사항

- 오버레이가 실제 클릭이나 드래그를 방해하지 않을 것
- 마우스 이동 시 끊김이 눈에 띄지 않을 것
- CPU와 메모리 사용량을 최소화할 것
- 다중 모니터와 DPI 배율 변경에 대응할 것
- 프로그램 비정상 종료 시 시스템 커서 설정을 훼손하지 않을 것
- 화면 녹화 프로그램에서 효과가 정상적으로 캡처될 것
- 전체 화면 또는 디스플레이 캡처를 공식 지원하며 특정 창 캡처는 공식 지원 범위에서 제외
- 일반 권한으로 실행하며 관리자 권한 프로그램의 입력 감지는 공식 지원 범위에서 제외

## 7. 개발 단계

1. 기술 검증
   - 완료된 결과는 `TECHNICAL_VALIDATION_RESULTS.md` 참조
   - 포인터 크기 임시 변경 및 복원
   - 투명 클릭 통과 오버레이
   - 전역 마우스 감지
   - 디스플레이 캡처 포함 여부
2. 기본 Spotlight 구현 (완료)
   - 마우스 추적
   - 고정 강조 원
   - 포인터 크기 적용 및 종료 시 복원
   - 시스템 트레이 및 종료 메뉴
3. 입력 효과 구현 (완료)
   - 클릭 점멸
   - 휠 방향별 애니메이션
4. 설정 및 트레이 구현 (완료)
5. 다중 모니터·DPI 대응
6. `spotlight.exe` 패키징 (완료)
7. 성능 및 호환성 테스트
8. 배포본 제작

## 8. 완료 기준

- `spotlight.exe`를 직접 실행할 수 있음
- 작업표시줄 없이 트레이에서 동작함
- 포인터 크기와 강조 원의 크기·색상·투명도를 조정할 수 있음
- 클릭 및 휠 효과가 설정대로 표시됨
- 설정이 재실행 후에도 유지됨
- Windows 10/11 및 다중 모니터 환경에서 정상 동작함
- 전체 화면 또는 디스플레이 캡처 방식으로 효과가 함께 녹화됨
