# pynput 제거 전환 지점 조사

> 당신은 Windows 데스크톱 애플리케이션 보안 개선 작업을 맡은 시니어 Python 개발자입니다.
>
> <프로젝트_배경>
> spotlight는 프레젠테이션용 마우스 커서 강조 도구입니다.
> Python 3.14 + PySide6(Qt) 기반이며 PyInstaller로 패키징됩니다.
> 주요 기능: 커서 주변 스포트라이트 오버레이, 클릭/휠 시각 이펙트, 시스템 커서 확대.
>
> 보안 검토 결과 이 앱이 사용하는 pynput 라이브러리가 문제로 지적되었습니다.
> - pynput은 SetWindowsHookEx(WH_MOUSE_LL) 전역 저수준 후크를 설치합니다.
> - pynput/__init__.py가 keyboard와 mouse를 모두 임포트하므로, 앱이 마우스만
>   사용해도 완전히 동작하는 키로거 코드(WH_KEYBOARD_LL, ToUnicodeEx,
>   GetAsyncKeyState, KeyTranslator)가 번들에 포함됩니다.
> - 다수 백신이 이를 키로거로 오탐하며, 무엇보다 앱이 불필요한 능력
>   (키 입력 캡처/차단/주입)을 보유하게 됩니다.
>
> 목표는 pynput을 완전히 제거하고 Windows Raw Input API로 대체하는 것입니다.
> </프로젝트_배경>
>
> <이번_단계_작업>
> 아직 코드를 작성하지 마십시오. 이번 단계는 사전 조사만 수행합니다.
>
> 1. spotlight/src/input.py 전문을 읽고 아래를 정확히 파악해 보고하십시오.
>    - 클래스명
>    - 정의된 Qt Signal 전체와 각각의 인자 타입/순서
>    - public 메서드와 시그니처, 반환형
>    - public 속성(상태 플래그 등)
>    - pynput API 사용 지점 전체
>
> 2. spotlight/src/app.py에서 입력 모듈과 상호작용하는 코드를 전부 찾아 보고하십시오.
>    - 시그널 연결부
>    - start/stop 호출부와 인자
>    - --report 출력에 포함되는 입력 관련 키
>    - 스레드 종료 대기 로직(join/timeout/is_alive 등) 유무
>
> 3. spotlight/src/overlay.py에서 add_click, add_wheel의 시그니처를 확인하십시오.
>
> 4. 빌드 설정 파일(.spec, pyproject.toml, requirements.txt 등)에서
>    pynput 관련 항목을 찾아 보고하십시오.
>
> 5. PySide6 정확한 버전을 확인하고, 해당 버전의 QWidget.nativeEvent()
>    시그니처와 반환 형식을 확인하십시오.
> </이번_단계_작업>
>
> <보고_형식>
> 아래 형식으로 간결하게 보고하십시오. 코드 전문을 그대로 붙여넣지 마십시오.
>
> ## 1. 현행 인터페이스 계약
> | 요소 | 실제 시그니처 | 비고 |
>
> ## 2. app.py 결합 지점
> (파일:라인 형식으로 위치와 내용)
>
> ## 3. overlay.py 수신부 시그니처
>
> ## 4. 빌드 설정 내 pynput 항목
>
> ## 5. PySide6 버전 및 nativeEvent 시그니처
>
> ## 6. 전환 시 예상 쟁점
> (내가 제시할 구현안과 충돌할 수 있는 부분)
> </보고_형식>
>
> 보고가 끝나면 멈추고 다음 지시를 기다리십시오.

<details><summary>6 previous messages</summary>

> 요청하신 범위만 읽기 전용으로 조사하겠습니다. 소스 결합 지점과 빌드 메타데이터를 대조한 뒤, 설치된 PySide6의 타입 스텁/런타임 정보까지 확인해 표 형식으로 정리하겠습니다.
>
> <details><summary>Ran a command</summary>
>
> - Ran `$files = rg --files | Where-Object { $_ -match '(^|[\\/])(input\.py|app\.py|overlay\.py|pyproject\.toml|requirements[^\\/]*\.txt|[^\\/]+\.spec|poetry\.lock|uv\.lock|Pipfile|setup\.(py|cfg))$' }; $files; Write-Output '--- INPUT ---'; Get-Content -LiteralPath 'spotlight/src/input.py'; Write-Output '--- APP MATCHES ---'; rg -n -C 4 "input|Input|click|wheel|start\(|stop\(|join\(|is_alive|report" 'spotlight/src/app.py'; Write-Output '--- OVERLAY METHODS ---'; rg -n -C 3 "def add_(click|wheel)" 'spotlight/src/overlay.py'; Write-Output '--- PYNPUT CONFIG ---'; rg -n -i "pynput|PySide6" -g '*.spec' -g 'pyproject.toml' -g 'requirements*.txt' -g 'poetry.lock' -g 'uv.lock' -g 'Pipfile*' -g 'setup.py' -g 'setup.cfg' .`
>
> </details>
>
> 프로젝트 루트 아래 실제 경로는 `spotlight/src/...`가 아니라 `src/spotlight/...`로 확인되었습니다. 동일한 모듈명 기준으로 계속 조사하며, 보고서에는 실제 파일 경로와 줄 번호를 사용하겠습니다.
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `Write-Output '--- INPUT NUMBERED ---'; $n=0; Get-Content -LiteralPath 'src/spotlight/input.py' | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ }; Write-Output '--- APP NUMBERED ---'; $n=0; Get-Content -LiteralPath 'src/spotlight/app.py' | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ }; Write-Output '--- OVERLAY RELEVANT ---'; rg -n -C 5 "def add_(click|wheel)" 'src/spotlight/overlay.py'; Write-Output '--- CONFIG FILE CONTENTS ---'; foreach ($f in @('requirements.txt','requirements-build.txt','spotlight.spec','packaging/spotlight_variants.spec','pyproject.toml')) { if (Test-Path -LiteralPath $f) { Write-Output ("--- " + $f); $n=0; Get-Content -LiteralPath $f | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ } } }; Write-Output '--- PYTHON/PYSIDE RUNTIME ---'; python -c "import sys; print(sys.version); import PySide6; print(PySide6.__version__); from PySide6.QtWidgets import QWidget; print(QWidget.nativeEvent.__doc__)"`
> - Ran `Write-Output '--- ENV CANDIDATES ---'; Get-ChildItem -Force | Select-Object Name,Mode; Write-Output '--- PYSIDE FILES ---'; rg --files -uu | rg "PySide6|pyside6|pyvenv\.cfg|nativeEvent" | Select-Object -First 100; Write-Output '--- ALL PYNPUT REFERENCES ---'; rg -n -i -uu "pynput" .; Write-Output '--- PY LAUNCHER ---'; py -0p`
> - Ran `rg -n -C 3 "def nativeEvent" 'validation/.venv/Lib/site-packages/PySide6/QtWidgets.pyi'; Get-Content -LiteralPath 'validation/.venv/Lib/site-packages/pyside6-6.11.1.dist-info/METADATA' -TotalCount 12; Write-Output '--- INPUT IMPORT REFERENCES ONLY ---'; rg -n "GlobalMouseInput|mouse_input|input_stopped|\.input import" src tests run_spotlight.py; Write-Output '--- CONFIG PYNPUT ONLY ---'; rg -n -i "pynput" requirements*.txt *.spec packaging/*.spec pyproject.toml setup.py setup.cfg Pipfile 2>$null`
>
> </details>
>
> 현행 인터페이스와 결합 지점은 모두 확인했습니다. `nativeEvent()`는 6.11.1 배포본의 `.pyi`가 반환형을 넓게 `object`로만 선언하므로, 실제 콜백 반환 규약은 Qt for Python 공식 문서와 대조해 확정하겠습니다.
>
> <details><summary>Searched the web</summary>
>
> Searched the web for `site:doc.qt.io/qtforpython-6.11 PySide6 QWidget nativeEvent eventType message return tuple`
>
> Searched the web for `site:doc.qt.io/qtforpython nativeEvent "return False, 0" PySide6 ...`
>
> </details>
</details>

## 1. 현행 인터페이스 계약

대상: [src/spotlight/input.py](./src/spotlight/input.py)

| 요소 | 실제 시그니처 | 비고 |
|---|---|---|
| 클래스 | `GlobalMouseInput(QObject)` | Qt `QObject` 상속 |
| Signal | `clicked = Signal(int, int, str)` | 순서: `x`, `y`, `button`; 버튼값은 `"left"` 또는 `"right"` |
| Signal | `scrolled = Signal(int, int, int)` | 순서: `x`, `y`, `delta`; `dy != 0`일 때만 발생 |
| 생성자 | `__init__(self) -> None` | `listener`, `started` 초기화 |
| public 메서드 | `start(self) -> None` | 이미 시작했으면 그대로 반환 |
| public 메서드 | `stop(self) -> bool` | 리스너 스레드 종료 성공 여부 반환 |
| public 속성 | `listener: mouse.Listener \| None` | pynput 리스너 인스턴스 |
| public 속성 | `started: bool` | 시작 상태 플래그 |

pynput 사용 지점:

- [input.py:4](./src/spotlight/input.py:4): `from pynput import mouse`
- [input.py:13](./src/spotlight/input.py:13): `mouse.Listener` 타입
- [input.py:19](./src/spotlight/input.py:19): `mouse.Listener(on_click=..., on_scroll=...)` 생성
- [input.py:23](./src/spotlight/input.py:23): `listener.start()`
- [input.py:31](./src/spotlight/input.py:31): `listener.stop()`
- [input.py:32](./src/spotlight/input.py:32): `listener.join(timeout=1.0)`
- [input.py:33](./src/spotlight/input.py:33): `listener.is_alive()`
- [input.py:41](./src/spotlight/input.py:41): `mouse.Button.left`
- [input.py:43](./src/spotlight/input.py:43): `mouse.Button.right`

비공개 콜백은 `_on_click(x, y, button, pressed) -> None`, `_on_scroll(x, y, dx, dy) -> None`입니다. 클릭은 눌림 이벤트만 처리하고 좌·우 버튼 이외는 무시합니다.

## 2. app.py 결합 지점

대상: [src/spotlight/app.py](./src/spotlight/app.py)

- [app.py:14](./src/spotlight/app.py:14): `GlobalMouseInput` 임포트
- [app.py:35](./src/spotlight/app.py:35): 인자 없이 `GlobalMouseInput()` 생성
- [app.py:36](./src/spotlight/app.py:36): `clicked.connect(self.overlay.add_click)`
- [app.py:37](./src/spotlight/app.py:37): `scrolled.connect(self.overlay.add_wheel)`
- [app.py:38](./src/spotlight/app.py:38): 앱 측 상태 `input_stopped = True`
- [app.py:46](./src/spotlight/app.py:46): `self.mouse_input.start()` — 인자 없음, 반환값 미사용
- [app.py:47](./src/spotlight/app.py:47): 시작 직후 `input_stopped = False`
- [app.py:54](./src/spotlight/app.py:54): `self.mouse_input.stop()` 반환값을 `input_stopped`에 저장
- [app.py:105](./src/spotlight/app.py:105): `--report` 관련 입력 키는 `"input_listener_started": self.mouse_input.started`

종료 대기 로직은 `app.py` 자체에는 없습니다. `join(timeout=1.0)`과 `is_alive()` 검사는 `GlobalMouseInput.stop()` 내부에 있습니다. 앱은 `aboutToQuit` 및 `finally` 양쪽에서 `stop()`을 호출하지만, `_stopped` 플래그로 중복 실행을 막습니다.

`input_stopped`는 report에 포함되지 않습니다.

## 3. overlay.py 수신부 시그니처

대상: [src/spotlight/overlay.py](./src/spotlight/overlay.py)

- [overlay.py:131](./src/spotlight/overlay.py:131): `add_click(self, x: int, y: int, button: str) -> None`
- [overlay.py:151](./src/spotlight/overlay.py:151): `add_wheel(self, x: int, y: int, delta: int) -> None`

`add_click()`은 `"left"`와 `"right"`만 허용합니다. `add_wheel()`은 `delta > 0` 여부로 방향을 판정하며, 0은 무시합니다.

## 4. 빌드 설정 내 pynput 항목

- [requirements.txt:2](./requirements.txt:2): `pynput==1.8.2`
- [requirements-build.txt:1](./requirements-build.txt:1): `-r requirements.txt`이므로 pynput을 간접 포함
- [spotlight.spec](./spotlight.spec): pynput 명시 항목이나 exclusion 없음
- [packaging/spotlight_variants.spec](./packaging/spotlight_variants.spec): pynput 명시 항목이나 필터링/exclusion 없음
- `pyproject.toml`, `setup.py`, `setup.cfg`, `Pipfile`은 존재하지 않음

기존 PyInstaller 분석 산출물에는 `pynput.keyboard._win32`를 포함한 키보드·마우스 모듈들이 실제 수집된 기록이 있습니다. 다만 이는 설정 파일이 아니라 과거 빌드 산출물입니다.

## 5. PySide6 버전 및 nativeEvent 시그니처

확정 버전은 **PySide6 6.11.1**입니다.

- [requirements.txt:1](./requirements.txt:1): `PySide6==6.11.1`
- 로컬 배포 메타데이터도 `Version: 6.11.1`

6.11.1 타입 스텁의 실제 선언:

```python
QWidget.nativeEvent(
    self,
    eventType: QByteArray | bytes | bytearray | memoryview,
    message: int,
    /,
) -> object
```

PySide 바인딩에서 재정의할 때의 실질 반환 형식은 `(handled: bool, result: int)` 튜플입니다. 처리하지 않은 메시지는 통상 `(False, 0)`, Windows 메시지를 소비하면 `(True, LRESULT)` 형태입니다. Windows에서 `message`는 `MSG *` 주소이며 이벤트 유형은 보통 `b"windows_generic_MSG"`입니다. 또한 해당 위젯에 네이티브 윈도우 핸들이 있어야 이벤트가 전달됩니다. [Qt for Python QWidget 문서](https://doc.qt.io/qtforpython-6/PySide6/QtWidgets/QWidget.html)

## 6. 전환 시 예상 쟁점

- `GlobalMouseInput`은 현재 `QObject`입니다. `QWidget.nativeEvent()`를 직접 사용하려면 네이티브 핸들을 가진 별도 `QWidget` 수신기나 기존 overlay와의 통합이 필요합니다.
- Raw Input 등록에는 유효한 `HWND`가 필요하므로 `start()` 호출 시점에 `winId()`가 생성되어 있는지 보장해야 합니다.
- 백그라운드 입력을 받으려면 `RIDEV_INPUTSINK`와 대상 `HWND` 설정이 필요합니다.
- Raw Input의 이동값은 기본적으로 상대 이동량입니다. 기존 signal 계약의 화면 좌표 `x, y`를 유지하려면 버튼/휠 발생 시 `GetCursorPos()` 등으로 좌표를 취득해야 합니다.
- 휠 값은 Raw Input에서 일반적으로 `WHEEL_DELTA` 단위(통상 ±120)입니다. 현재 pynput의 `dy` 값과 크기가 달라질 수 있지만 overlay는 부호만 사용합니다.
- 현재는 좌·우 버튼의 press만 전달하고 release, 가운데/X 버튼, 수평 휠은 무시합니다. 이 필터링 계약을 유지해야 합니다.
- pynput 콜백은 별도 리스너 스레드에서 signal을 emit하지만 `nativeEvent()`는 Qt GUI 스레드에서 실행됩니다. signal 인터페이스는 같아도 전달 시점과 연결 방식이 달라질 수 있습니다.
- 새 구현에는 리스너 스레드가 없을 가능성이 높습니다. 따라서 `stop() -> bool`, `started`, 앱의 `input_stopped`, report 키 이름 `input_listener_started`의 의미를 어떻게 유지할지 결정해야 합니다.
- 종료 시 `join()`은 불필요해지는 대신 Raw Input 등록 해제와 수신용 네이티브 윈도우 수명 관리가 필요합니다.
- 코드 의존성을 제거한 뒤 requirements뿐 아니라 깨끗한 PyInstaller 재빌드로 과거 pynput 분석·번들 산출물이 남지 않았는지 확인해야 합니다.

코드는 변경하지 않았습니다.

> 이제 전환을 구현합니다.
>
> <작업_범위>
> 반드시 spotlight/input.py 하나만 전면 교체하십시오.
> app.py는 프롬프트 1에서 확인된 불일치가 있을 때만, 최소한으로 수정합니다.
> overlay.py, cursor.py, config.py, settings_dialog.py, tray.py는
> 어떤 경우에도 수정하지 마십시오.
>
> 예외: 아래 <대안_경로>가 발동한 경우에만 overlay.py 수정을 허용합니다.
> </작업_범위>
>
> <필수_계약>
> 프롬프트 1에서 당신이 확인한 실제 시그니처를 그대로 유지하십시오.
> 아래는 내가 정적분석으로 추정한 값입니다. 실제 소스와 다르면 실제 소스가 우선입니다.
>
>   clicked  = Signal(int, int, str, bool)   # (x, y, "left"|"right", pressed)
>   scrolled = Signal(int, int, int, int)    # (x, y, dx, dy)
>   start()  -> None
>   stop()   -> bool
>   started  : bool 속성
>
> 버튼명 문자열은 현행과 동일하게 유지하십시오.
> </필수_계약>
>
> <기술_사양>
> Raw Input API를 ctypes로 직접 호출합니다. 외부 라이브러리를 추가하지 마십시오.
>
> 필요 상수:
>   HID_USAGE_PAGE_GENERIC  = 0x01
>   HID_USAGE_GENERIC_MOUSE = 0x02
>   RIDEV_INPUTSINK = 0x00000100
>   RIDEV_REMOVE    = 0x00000001
>   WM_INPUT        = 0x00FF
>   RID_INPUT       = 0x10000003
>   RIM_TYPEMOUSE   = 0
>   RI_MOUSE_LEFT_BUTTON_DOWN  = 0x0001
>   RI_MOUSE_LEFT_BUTTON_UP    = 0x0002
>   RI_MOUSE_RIGHT_BUTTON_DOWN = 0x0004
>   RI_MOUSE_RIGHT_BUTTON_UP   = 0x0008
>   RI_MOUSE_WHEEL             = 0x0400
>   RI_MOUSE_HWHEEL            = 0x0800
>   WHEEL_DELTA                = 120
>
> 필요 구조체: RAWINPUTDEVICE, RAWINPUTHEADER, RAWMOUSE, RAWINPUT
>   RAWMOUSE는 usButtonFlags/usButtonData를 담는 익명 union을 포함합니다.
>   필드 순서: usFlags(USHORT), union, ulRawButtons(ULONG),
>              lLastX(LONG), lLastY(LONG), ulExtraInformation(ULONG)
>
> 구현 흐름:
> 1. WM_INPUT 수신용 숨김 QWidget을 input.py 내부에 정의
>    - Qt.WA_DontShowOnScreen 설정
>    - winId() 호출로 네이티브 핸들 강제 생성
>    - nativeEvent() 오버라이드하여 WM_INPUT만 처리
> 2. RegisterRawInputDevices로 해당 hwnd에 마우스 등록 (RIDEV_INPUTSINK)
> 3. GetRawInputData 2회 호출 규약으로 RAWMOUSE 추출
>    (1차 pData=None으로 크기 조회, 2차로 데이터 수신)
> 4. usButtonFlags 비트 검사로 클릭/휠 판별
> 5. 좌표는 GetCursorPos()로 별도 취득
> 6. stop() 시 RIDEV_REMOVE로 해제하고 창 정리
> </기술_사양>
>
> <반드시_지킬_것>
> 1. RIDEV_INPUTSINK를 반드시 지정하십시오. 이것이 없으면 앱이 포그라운드일
>    때만 입력을 받습니다. 프레젠테이션 중에는 PowerPoint가 전면에 있으므로
>    앱이 무용지물이 됩니다. 이 앱의 핵심 요구사항입니다.
>
> 2. 휠 델타는 반드시 ctypes.c_short()로 부호 변환하십시오.
>    usButtonData는 구조체상 USHORT이지만 실제 값은 부호 있는 SHORT입니다.
>    변환하지 않으면 아래 스크롤의 -120이 65416으로 읽혀 방향이 역전됩니다.
>
> 3. nativeEvent()는 반드시 (False, 0)을 반환하십시오.
>    True를 반환하면 메시지가 소비되어 Qt 내부 처리가 중단됩니다.
>
> 4. RIDEV_REMOVE로 해제할 때 hwndTarget은 반드시 NULL이어야 합니다.
>
> 5. GetRawInputData는 반드시 2회 호출하십시오. 1회 호출은 버퍼 부족으로
>    실패합니다.
>
> 6. RegisterRawInputDevices는 winId() 호출 이후에만 수행하십시오.
>
> 7. ctypes.WinDLL("user32", use_last_error=True)를 사용하고,
>    등록 실패 시 ctypes.WinError(ctypes.get_last_error())를 발생시키십시오.
>
> 8. 기존 코드의 타입 힌트 스타일, docstring 유무, 네이밍 컨벤션을
>    그대로 따르십시오.
> </반드시_지킬_것>
>
> <금지사항>
> 1. RIDEV_NOLEGACY를 사용하지 마십시오. 일반 마우스 메시지 생성을 차단해
>    설정 창 등 앱의 다른 UI가 동작하지 않게 됩니다.
>
> 2. 구조체에 _pack_을 지정하지 마십시오. usFlags 뒤 정렬 패딩을 ctypes가
>    자동 처리하므로, _pack_을 지정하면 오히려 구조가 어긋납니다.
>
> 3. 별도 스레드나 자체 메시지 루프를 만들지 마십시오. Qt의 기존 이벤트
>    루프를 사용하는 것이 이 전환의 핵심 이점입니다.
>
> 4. pynput을 조건부 임포트나 폴백으로 남겨두지 마십시오. 완전히 제거해야
>    보안 목표가 달성됩니다.
>
> 5. 요청하지 않은 기능을 추가하지 마십시오. 마우스 이동 추적, 로깅,
>    설정 항목 추가 등은 범위 밖입니다.
>
> 6. 기존 시그널명이나 메서드명을 "더 나은 이름"으로 바꾸지 마십시오.
>    롤백 가능성을 위해 계약을 유지해야 합니다.
> </금지사항>
>
> <대안_경로>
> 숨김 QWidget이 WM_INPUT을 수신하지 못하는 경우가 있습니다.
> 이 경우에만 다음을 수행하고, 수행했다면 반드시 보고하십시오.
> - 등록 대상을 SpotlightOverlay 위젯의 winId()로 변경
> - overlay.py에 nativeEvent() 오버라이드 추가
> - input.py가 overlay 인스턴스를 주입받도록 변경
> </대안_경로>
>
> <작업_순서>
> 1. 새 input.py를 작성합니다.
> 2. 프롬프트 1에서 발견된 불일치가 있으면 app.py를 최소 수정합니다.
> 3. 빌드 설정에서 pynput 의존성을 제거하고 다음을 추가합니다.
>    --exclude-module pynput
>    --exclude-module pynput.keyboard
>    --exclude-module pynput.mouse
> 4. 변경 사항을 요약 보고합니다.
> </작업_순서>
>
> <보고_형식>
> 구현 후 아래를 보고하십시오.
> - 변경한 파일 목록과 각 파일의 변경 요지
> - 프롬프트 1의 계약과 달라진 부분이 있다면 그 이유
> - <대안_경로> 발동 여부
> - 내가 제시한 사양 중 실제 환경과 맞지 않아 조정한 부분
> - 실행 검증이 필요한 항목
> </보고_형식>
>
> <불확실할_때>
> 추측해서 진행하지 말고 질문하십시오. 특히 다음의 경우 반드시 멈추고 물으십시오.
> - 실제 시그니처가 내가 제시한 추정과 크게 다를 때
> - PySide6 버전 때문에 nativeEvent 구현 방식을 바꿔야 할 때
> - app.py 수정 범위가 "최소"를 넘어설 때
> - 내 사양대로 하면 기존 기능이 깨질 것으로 판단될 때
> </불확실할_때>

실제 시그니처가 제시하신 추정과 크게 달라 구현을 중단했습니다.

현행 계약은 다음과 같습니다.

- `clicked = Signal(int, int, str)` — press만 전달
- `scrolled = Signal(int, int, int)` — 수직 휠 `dy`만 전달
- `overlay.add_click(x, y, button)`
- `overlay.add_wheel(x, y, delta)`

실제 소스 우선 원칙에 따라 이 3인자 계약을 그대로 유지하고, 버튼 release 및 수평 휠은 현행처럼 무시하는 방향으로 구현하면 될까요? 이렇게 하면 `app.py`와 `overlay.py` 수정은 필요하지 않습니다.

> 네 현재 함수 유지하도록 합시다.

<details><summary>8 previous messages</summary>

> 확인된 3인자 계약을 유지하겠습니다. `input.py`는 숨김 네이티브 QWidget 기반으로 교체하고, `app.py`/`overlay.py`는 건드리지 않겠습니다. 이후 requirements와 두 PyInstaller spec에서 pynput 제거·명시적 제외를 적용한 뒤 정적 검증을 진행하겠습니다.
>
> <details><summary>Created a file, edited 3 files, deleted a file, ran a command</summary>
>
> - Ran `Get-Content -LiteralPath 'src/spotlight/input.py'; Write-Output '--- GIT STATUS ---'; git status --short; Write-Output '--- BUILD SCRIPT ---'; $n=0; Get-Content -LiteralPath 'build.ps1' | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ }; Write-Output '--- TEST INPUT REFERENCES ---'; rg -n "GlobalMouseInput|clicked|scrolled|input_listener_started|pynput" tests src requirements*.txt *.spec packaging/*.spec build.ps1`
> - Deleted `./src/spotlight/input.py` (+0 -49)
> - Wrote `./packaging/spotlight_variants.spec` (+1 -1)
> - Wrote `./requirements.txt` (+0 -1)
> - Wrote `./spotlight.spec` (+1 -1)
> - Wrote `./src/spotlight/input.py` (+199 -0)
>
> </details>
>
> 핵심 교체 패치를 적용 중입니다. 적용이 끝나는 즉시 구조체 크기·정렬, WinAPI 호출 인자, 기존 signal 연결 호환성을 별도로 검사하겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `git status --short; git diff -- src/spotlight/input.py requirements.txt spotlight.spec packaging/spotlight_variants.spec; Write-Output '--- STATIC SEARCH ---'; rg -n -i "pynput|RIDEV_NOLEGACY|RIDEV_INPUTSINK|RIDEV_REMOVE|GetRawInputData|nativeEvent|Signal\(" src requirements.txt requirements-build.txt spotlight.spec packaging/spotlight_variants.spec`
> - Ran `& '.\validation\.venv\Scripts\python.exe' -c "import ast, pathlib; p=pathlib.Path('src/spotlight/input.py'); ast.parse(p.read_text(encoding='utf-8')); print('syntax ok')"; & '.\validation\.venv\Scripts\python.exe' -c "import sys; sys.path.insert(0, 'src'); from PySide6.QtWidgets import QApplication; from spotlight.input import RAWINPUTDEVICE, RAWINPUTHEADER, RAWMOUSE, RAWINPUT; import ctypes; print(ctypes.sizeof(RAWINPUTDEVICE), ctypes.sizeof(RAWINPUTHEADER), ctypes.sizeof(RAWMOUSE), ctypes.sizeof(RAWINPUT))"`
>
> </details>
>
> 패치는 적용됐고, 계약상 `app.py`와 금지된 모듈들은 변경되지 않았습니다. 현재 로컬 검증용 가상환경은 삭제된 Python 3.11 경로를 가리켜 실행할 수 없으므로, 프로젝트가 실제 사용한 Python 3.14 실행 파일로 구문·구조체 크기와 최소 수명주기를 확인해 보겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import ast, ctypes, pathlib, sys; p=pathlib.Path('src/spotlight/input.py'); ast.parse(p.read_text(encoding='utf-8')); sys.path.insert(0, 'src'); from spotlight.input import RAWINPUTDEVICE, RAWINPUTHEADER, RAWMOUSE, RAWINPUT; print('syntax ok'); print(ctypes.sizeof(RAWINPUTDEVICE), ctypes.sizeof(RAWINPUTHEADER), ctypes.sizeof(RAWMOUSE), ctypes.sizeof(RAWINPUT))"`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import sys; sys.path.insert(0, 'src'); from PySide6.QtWidgets import QApplication; from spotlight.input import GlobalMouseInput; app=QApplication([]); mouse=GlobalMouseInput(); mouse.start(); print('started', mouse.started, 'hwnd', int(mouse.widget.winId())); print('stopped', mouse.stop(), 'state', mouse.started)"; git diff --check; Write-Output '--- FORBIDDEN FILE STATUS ---'; git status --short -- src/spotlight/app.py src/spotlight/overlay.py src/spotlight/cursor.py src/spotlight/config.py src/spotlight/settings_dialog.py src/spotlight/tray.py; Write-Output '--- TRACKED PYNPUT REFERENCES ---'; git grep -n -i pynput -- ':!build/**' ':!dist/**' ':!validation/**'`
>
> </details>
>
> PySide6 6.11.1 실환경 검증에서 문서/타입 스텁과 달리 `message`가 `int`가 아니라 `shiboken6.Shiboken.VoidPtr`로 전달되는 것이 확인됐습니다. 구현 구조를 바꿀 필요는 없고, 주소를 `int(message)`로 명시 변환해야 하는 바인딩 차이이므로 그 최소 조정만 적용합니다. 이는 최종 보고에 명시하겠습니다.
>
> <details><summary>Edited a file, ran a command</summary>
>
> - Wrote `./src/spotlight/input.py` (+3 -1)
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import sys; sys.path.insert(0, 'src'); from PySide6.QtWidgets import QApplication; from spotlight.input import GlobalMouseInput; app=QApplication([]); mouse=GlobalMouseInput(); mouse.start(); print('started', mouse.started, 'hwnd', int(mouse.widget.winId())); app.processEvents(); print('stopped', mouse.stop(), 'state', mouse.started)"; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -m pytest -q`
>
> </details>
</details>

구현 완료했습니다.

- [input.py](./src/spotlight/input.py): pynput을 완전히 제거하고 ctypes 기반 Windows Raw Input으로 교체
- [requirements.txt](./requirements.txt): `pynput==1.8.2` 제거
- [spotlight.spec](./spotlight.spec): pynput 및 keyboard/mouse 하위 모듈 제외
- [spotlight_variants.spec](./packaging/spotlight_variants.spec): 동일한 모듈 제외 적용

`app.py`, `overlay.py`와 나머지 금지된 파일은 수정하지 않았습니다.

계약은 현행 그대로 유지했습니다.

- `clicked(int, int, str)`: 좌·우 버튼 down만 전달
- `scrolled(int, int, int)`: 수직 휠만 전달하며 ±120을 기존 pynput과 같은 ±1 단위로 변환
- `start() -> None`
- `stop() -> bool`
- `started: bool`

대안 경로는 발동하지 않았습니다. 숨김 QWidget의 네이티브 핸들 생성, `RIDEV_INPUTSINK` 등록 및 `RIDEV_REMOVE` 해제가 실제 환경에서 성공했습니다.

PySide6 6.11.1은 타입 스텁과 달리 `nativeEvent()`의 `message`를 `Shiboken.VoidPtr`로 전달했습니다. 따라서 `int(message)`로 주소를 명시 변환한 뒤 `MSG*`로 캐스팅했습니다. 반환값은 항상 `(False, 0)`입니다.

검증 결과:

- Python 3.14 구문 검사 통과
- 64비트 구조체 크기 확인: `RAWINPUTDEVICE=16`, `RAWINPUTHEADER=24`, `RAWMOUSE=24`, `RAWINPUT=48`
- 숨김 창 생성 및 Raw Input 등록 성공
- `stop()` 등록 해제 성공, `started=False` 확인
- `git diff --check` 통과
- pytest는 현재 Python 3.14 환경에 설치되어 있지 않아 실행하지 못했습니다.

추가 실행 검증이 필요한 항목은 실제 좌·우 클릭, 상·하 휠 방향, PowerPoint가 전면인 상태에서의 백그라운드 수신, PyInstaller 클린 빌드 결과물에서 pynput 모듈이 제외됐는지 여부입니다.

> 전환이 정상 완료되었는지 검증합니다.
>
> <검증_1_문자열_소거>
> 빌드된 실행파일에서 pynput 잔재가 완전히 제거되었는지 확인하는
> Python 스크립트를 작성해 실행하십시오.
>
> 동작:
> 1. PyInstaller 실행파일에서 PYZ.pyz를 추출
>    (pyinstxtractor 사용 또는 직접 파싱)
> 2. PYZ TOC를 파싱해 각 모듈을 zlib 압축해제
> 3. 아래 문자열이 하나라도 남아 있는지 검사
>
>      SetWindowsHookEx, GetAsyncKeyState, ToUnicodeEx,
>      GetKeyboardLayout, KeyTranslator, WH_KEYBOARD_LL,
>      KBDLLHOOKSTRUCT, MSLLHOOKSTRUCT, pynput
>
> 4. 검출된 문자열과 소재 모듈명을 출력
>
> 판정: 검출 0건이어야 전환 완료입니다.
> 검출이 있으면 어느 모듈에서 나왔는지 보고하고 원인을 분석하십시오.
>
> 참고: PYZ 구조는 magic(4) + pyc_magic(4) + toc_offset(4, big-endian int)
> 이며, toc_offset 위치에서 marshal.load()로 TOC를 읽습니다.
> TOC 각 항목은 (ispkg, pos, length) 튜플입니다.
> </검증_1_문자열_소거>
>
> <검증_2_기능_체크리스트>
> 아래 항목을 수동 테스트할 수 있도록 체크리스트 문서를 작성하십시오.
> 각 항목에 판정 기준을 명시할 것.
>
> F-1 좌클릭 이펙트
> F-2 우클릭 이펙트
> F-3 휠 방향 (위/아래 각 5회 — 방향별로 이펙트가 달라야 함)
> F-4 PowerPoint 슬라이드쇼 실행 중 클릭 감지  ← 최우선
> F-5 설정 창 조작 (트레이 → 설정 → 값 변경 → 적용)
> F-6 시작/중지 10회 반복
> F-7 정상 종료 후 재실행
>
> E-1 RDP 또는 VDI 환경에서 입력 수신
> E-2 고DPI·다중 모니터 간 좌표 정합성
> E-3 고폴링(1000Hz) 마우스 연결 시 CPU 사용률
> E-4 --report 출력의 cursor_count_applied 값이 13인지 확인
> </검증_2_기능_체크리스트>
>
> <보고_형식>
> - 검증 1 결과 (검출 문자열 유무)
> - 검증 2 체크리스트 파일 경로
> - 추가로 확인이 필요하다고 판단되는 항목
> </보고_형식>

<details><summary>10 previous messages</summary>

> 현재 `dist`가 전환 전 산출물일 수 있으므로 새 설정으로 클린 빌드한 실행파일을 대상으로 검사하겠습니다. 검증 스크립트는 CArchive에서 `PYZ.pyz`를 꺼낸 뒤 요청하신 헤더/TOC 규약을 직접 파싱하도록 만들고, 수동 체크리스트는 기존 `validation` 디렉터리에 추가하겠습니다.
>
> <details><summary>Created 2 files, ran 2 commands</summary>
>
> - Ran `Write-Output '--- DIST ---'; Get-ChildItem -LiteralPath dist -Force -ErrorAction SilentlyContinue | Select-Object Name,Length,LastWriteTime; Write-Output '--- VALIDATION FILES ---'; Get-ChildItem -LiteralPath validation -Force | Select-Object Name,Mode; Write-Output '--- ARCHIVE API ---'; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import inspect; from PyInstaller.archive.readers import CArchiveReader; print(inspect.signature(CArchiveReader)); print([x for x in dir(CArchiveReader) if not x.startswith('_')]); print(inspect.signature(CArchiveReader.extract))"`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import inspect; from PyInstaller.archive.readers import CArchiveReader; print(inspect.getsource(CArchiveReader.extract)); print(inspect.getsource(CArchiveReader.__init__))"`
> - Wrote `./validation/RAW_INPUT_MANUAL_CHECKLIST.md` (+26 -0)
> - Wrote `./validation/scan_pyz_strings.py` (+82 -0)
>
> </details>
>
> 검증 스크립트와 체크리스트를 작성했습니다. 이제 기존 산출물을 신뢰하지 않고 PyInstaller `--clean` 빌드를 수행한 뒤, 새 `dist/spotlight.exe`의 PYZ 모듈 전체를 검사하겠습니다.
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' packaging/create_icon.py; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -m PyInstaller --noconfirm --clean spotlight.spec`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' validation/scan_pyz_strings.py dist/spotlight.exe; $scanExit = $LASTEXITCODE; Write-Output "SCAN_EXIT=$scanExit"; Write-Output '--- ANALYSIS PYNPUT ---'; rg -n -i "pynput|SetWindowsHookEx|GetAsyncKeyState|ToUnicodeEx|GetKeyboardLayout|KeyTranslator|WH_KEYBOARD_LL|KBDLLHOOKSTRUCT|MSLLHOOKSTRUCT" build/spotlight/Analysis-00.toc build/spotlight/PYZ-00.toc build/spotlight/xref-spotlight.html build/spotlight/warn-spotlight.txt; exit $scanExit`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import io,marshal,struct,sys; sys.path.insert(0,'validation'); from pathlib import Path; from scan_pyz_strings import extract_pyz; d=extract_pyz(Path('dist/spotlight.exe')); s=io.BytesIO(d); s.seek(struct.unpack('>I',d[8:12])[0]); t=marshal.load(s); print(type(t), len(t)); print(t[:3] if isinstance(t,list) else list(t.items())[:3])"`
>
> </details>
>
> 클린 빌드는 성공했습니다. 다만 PyInstaller 6.22의 실제 PYZ TOC는 참고 형식과 달리 `dict`가 아니라 `(모듈명, 엔트리)` 쌍의 `list`로 직렬화되어 있었습니다. 엔트리 자체는 요청하신 `(ispkg, pos, length)` 형식 그대로이므로, 두 TOC 컨테이너 형식을 모두 처리하도록 검사기만 조정합니다.
>
> <details><summary>Edited a file, ran a command</summary>
>
> - Wrote `./validation/scan_pyz_strings.py` (+2 -1)
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' validation/scan_pyz_strings.py dist/spotlight.exe; $scanExit = $LASTEXITCODE; Write-Output "SCAN_EXIT=$scanExit"; Write-Output '--- BUILD MODULE NAMES ---'; rg -n "\('pynput|pynput\." build/spotlight/Analysis-00.toc build/spotlight/PYZ-00.toc; exit $scanExit`
>
> </details>
>
> 새 실행파일의 PYZ 151개 모듈을 모두 압축 해제해 검사한 결과 검출은 0건입니다. 마지막으로 패키징된 실행파일 자체의 smoke report를 실행해 Raw Input 시작과 커서 적용 수치도 함께 확인하겠습니다.
>
> <details><summary>Ran a command</summary>
>
> - Ran `& '.\dist\spotlight.exe' --smoke-test --report '.\validation\artifacts\raw-input-report.json'; $exitCode = $LASTEXITCODE; Write-Output "APP_EXIT=$exitCode"; Get-Content -LiteralPath '.\validation\artifacts\raw-input-report.json'; Write-Output '--- DIFF CHECK ---'; git diff --check; Write-Output '--- STATUS ---'; git status --short`
>
> </details>
>
> GUI 서브시스템 실행파일이라 PowerShell의 직접 호출이 프로세스 종료를 기다리지 않아 첫 report 확인이 너무 일찍 실행됐습니다. 프로세스 대기 방식으로 다시 실행해 실제 종료 코드와 JSON 생성 여부를 확인하겠습니다.
>
> <details><summary>Ran 3 commands</summary>
>
> - Ran `$exePath = (Resolve-Path '.\dist\spotlight.exe').Path; $reportPath = Join-Path (Resolve-Path '.\validation\artifacts').Path 'raw-input-report.json'; $process = Start-Process -FilePath $exePath -ArgumentList @('--smoke-test', '--report', $reportPath) -WindowStyle Hidden -Wait -PassThru; Write-Output "APP_EXIT=$($process.ExitCode)"; Get-Content -LiteralPath $reportPath`
> - Ran `$artifactDir = (Resolve-Path '.\validation\artifacts').Path; $exePath = (Resolve-Path '.\dist\spotlight.exe').Path; $reportPath = Join-Path $artifactDir 'raw-input-report.json'; $settingsPath = Join-Path $artifactDir 'raw-input-settings.ini'; $recoveryPath = Join-Path $artifactDir 'raw-input-recovery.json'; $process = Start-Process -FilePath $exePath -ArgumentList @('--smoke-test', '--report', $reportPath, '--settings-path', $settingsPath, '--recovery-path', $recoveryPath) -WindowStyle Hidden -Wait -PassThru; Write-Output "APP_EXIT=$($process.ExitCode)"; Get-Content -LiteralPath $reportPath`
> - Ran `Get-Process -Name 'spotlight' -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime; Write-Output '--- FINAL SCAN ---'; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' validation/scan_pyz_strings.py dist/spotlight.exe; Write-Output '--- DIFF CHECK ---'; git diff --check`
>
> </details>
</details>

- 검증 1 결과: **검출 0건 — 통과**
  - 클린 빌드: PyInstaller 6.22.0 / Python 3.14.7
  - 대상: `dist/spotlight.exe`
  - 검사한 PYZ 모듈: 151개
  - 검사 스크립트: [scan_pyz_strings.py](./validation/scan_pyz_strings.py)
  - `SetWindowsHookEx`, `GetAsyncKeyState`, `ToUnicodeEx`, `GetKeyboardLayout`, `KeyTranslator`, `WH_KEYBOARD_LL`, `KBDLLHOOKSTRUCT`, `MSLLHOOKSTRUCT`, `pynput` 모두 미검출

- 검증 2 체크리스트: [RAW_INPUT_MANUAL_CHECKLIST.md](./validation/RAW_INPUT_MANUAL_CHECKLIST.md)

추가 확인 사항:

- 패키징 실행파일의 smoke test는 정상 종료했고 `input_listener_started=true`, 커서 복구 성공을 확인했습니다.
- E-4의 `cursor_count_applied`는 요구값 `13`이 아니라 현재 환경에서 **14**로 출력되어 판정 기준상 실패입니다. Raw Input과 별개인 시스템 커서 개수 차이로 보이며 추가 조사가 필요합니다.
- `active_cursor_image_size`도 현재 smoke report에서 `null`이었습니다.
- F-4 PowerPoint 전체 화면 백그라운드 입력, 실제 휠 방향, RDP/VDI 및 다중 모니터 좌표는 사람이 실제 입력 환경에서 확인해야 합니다.

> 진행해

<details><summary>4 previous messages</summary>

> 자동으로 검증 가능한 항목부터 계속 진행하겠습니다. 우선 E-4의 `cursor_count_applied=14`와 `active_cursor_image_size=null` 원인을 확인하고, 실제 마우스 조작이 필요한 F-1~F-7은 실행 가능한 테스트 상태까지 준비하겠습니다.
>
> <details><summary>Ran a command</summary>
>
> - Ran `$n=0; Get-Content -LiteralPath 'src/spotlight/cursor.py' | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ }; Write-Output '--- CURSOR TESTS ---'; $n=0; Get-Content -LiteralPath 'tests/test_cursor.py' | ForEach-Object { $n++; '{0,4}: {1}' -f $n, $_ }; Write-Output '--- REPORT ARTIFACT ---'; Get-Content -LiteralPath 'validation/artifacts/raw-input-report.json' -ErrorAction SilentlyContinue; Write-Output '--- CURSOR PROBE REFERENCES ---'; rg -n "cursor_count_applied|active_cursor_image_size|applied_count|SYSTEM_CURSOR|OCR_|LoadCursor|SetSystemCursor" src tests validation`
>
> </details>
>
> E-4의 14는 오류성 누적이 아니라 `SYSTEM_CURSORS`에 실제로 14개 항목이 정의되어 있어 나온 값입니다. 따라서 체크리스트의 기대값 13이 현재 코드 계약과 불일치합니다. 소스는 수정하지 않고, 나머지 자동 검증으로 Raw Input 등록/해제 10회 반복과 표준 라이브러리 테스트 전체를 실행하겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import sys; sys.path.insert(0, 'src'); from PySide6.QtWidgets import QApplication; from spotlight.input import GlobalMouseInput; app=QApplication([]); mouse=GlobalMouseInput(); results=[]; [results.append((mouse.start(), mouse.started, mouse.stop(), mouse.started)) for _ in range(10)]; print(results); assert all(started and stopped and not final for _, started, stopped, final in results)"; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -m unittest discover -s tests -v`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "import sys; sys.path.insert(0, 'src'); from spotlight.cursor import active_cursor_image_size, SYSTEM_CURSORS; print('system_cursor_count', len(SYSTEM_CURSORS)); print('active_cursor_image_size', active_cursor_image_size())"; Get-Process -Name spotlight -ErrorAction SilentlyContinue | Select-Object Id,ProcessName`
>
> </details>
</details>

자동 검증을 추가 진행했습니다.

- Raw Input `start()`/`stop()` 10회 반복: 전부 성공
  - 매회 `started=True`
  - 해제 반환값 `True`
  - 해제 후 `started=False`
- 전체 단위 테스트: 17개 모두 통과
- 잔류 `spotlight.exe` 프로세스 없음
- `cursor_count_applied=14` 원인 확정:
  - [cursor.py](./src/spotlight/cursor.py:18)의 `SYSTEM_CURSORS`가 실제로 14개
  - 따라서 E-4의 기대값 13은 현재 구현과 불일치
- `active_cursor_image_size=None`은 별도 실행에서도 재현됨

남은 F-1~F-5, F-7 및 E-1~E-3은 실제 마우스·PowerPoint·RDP·다중 모니터 환경에서 [수동 체크리스트](./validation/RAW_INPUT_MANUAL_CHECKLIST.md)에 따라 확인해야 합니다. 특히 F-4 PowerPoint 슬라이드쇼 검증이 최우선입니다.

> 실행파일의 크기가 45MB인데, 너무 크거든... 이걸 좀 줄일 수 있는 방법이 없을까. 사용하지 않는 라이브러리까지 다 들어있는거 아니야? 이건 보안검토에도 좋지 않은 영향을 줄 것 같아

<details><summary>6 previous messages</summary>

> 가능성이 큽니다. 단일 파일 PyInstaller 빌드에는 Qt 플랫폼 플러그인과 DLL이 함께 들어가므로, 실제 사용 모듈보다 훨씬 커질 수 있습니다. 현재 45MB 실행파일의 내부 항목을 크기순으로 분석하고, 이미 있는 `safe/minimal` 변형 설정이 무엇을 제거하는지도 대조해 보겠습니다. 지금은 수정 없이 조사만 하겠습니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `$exe = Get-Item -LiteralPath 'dist/spotlight.exe'; Write-Output ("EXE_BYTES=" + $exe.Length); Get-ChildItem -LiteralPath 'dist/variants' -File -ErrorAction SilentlyContinue | Select-Object Name,Length,LastWriteTime; Write-Output '--- SOURCE PYSIDE IMPORTS ---'; rg -n "from PySide6|import PySide6" src run_spotlight.py; Write-Output '--- SPEC FILTERS ---'; Get-Content -LiteralPath 'packaging/spotlight_variants.spec'; Write-Output '--- BUILD DOC VARIANTS ---'; rg -n -C 3 "variant|minimal|safe|PyInstaller|size|크기" README.md docs packaging validation *.md`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "from PyInstaller.archive.readers import CArchiveReader; a=CArchiveReader('dist/spotlight.exe'); rows=[(v[1],v[2],v[4],k) for k,v in a.toc.items()]; print('entries',len(rows),'compressed_MB',round(sum(x[0] for x in rows)/1048576,2),'raw_MB',round(sum(x[1] for x in rows)/1048576,2)); [print(f'{c/1048576:7.2f} MB <- {u/1048576:7.2f} MB [{t}] {n}') for c,u,t,n in sorted(rows,reverse=True)[:40]]"`
>
> </details>
>
> 분석 결과 45MB 중 가장 큰 불필요 후보는 `opengl32sw.dll` 7.29MB, `Qt6Quick` 2.78MB, `Qt6Pdf` 2.35MB, `Qt6Qml` 2.05MB이며 앱 소스에서는 이 기능들을 사용하지 않습니다. 프로젝트에 이미 이를 제거하는 `safe/minimal` 실험 spec이 있으므로, 현재 코드로 세 변형을 다시 빌드해 실제 크기와 기동 가능 여부를 비교하겠습니다.
>
> <details><summary>Ran a command</summary>
>
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -m PyInstaller --noconfirm --clean --distpath dist/variants --workpath build/variants packaging/spotlight_variants.spec`
>
> </details>
>
> 세 변형 빌드가 완료됐습니다. 크기만 줄고 기능이 깨지는 경우를 걸러내기 위해 각 실행파일에 smoke test와 pynput PYZ 검사를 적용하겠습니다. 특히 minimal에서 설정 창·트레이에 필요한 Qt 플러그인이 빠지지 않았는지 확인합니다.
>
> <details><summary>Ran 2 commands</summary>
>
> - Ran `$artifactDir = (Resolve-Path '.\validation\artifacts').Path; Get-ChildItem -LiteralPath 'dist/variants' -Filter 'spotlight-*.exe' | Sort-Object Length -Descending | ForEach-Object { $name = $_.BaseName; $report = Join-Path $artifactDir ($name + '-report.json'); $settings = Join-Path $artifactDir ($name + '-settings.ini'); $recovery = Join-Path $artifactDir ($name + '-recovery.json'); $proc = Start-Process -FilePath $_.FullName -ArgumentList @('--smoke-test','--report',$report,'--settings-path',$settings,'--recovery-path',$recovery) -WindowStyle Hidden -Wait -PassThru; [pscustomobject]@{Name=$_.Name; SizeMB=[math]::Round($_.Length/1MB,2); ExitCode=$proc.ExitCode; ReportExists=(Test-Path -LiteralPath $report)} } | Format-Table -AutoSize; Write-Output '--- PYZ SCANS ---'; Get-ChildItem -LiteralPath 'dist/variants' -Filter 'spotlight-*.exe' | ForEach-Object { & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' validation/scan_pyz_strings.py $_.FullName }`
> - Ran `& 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "from PyInstaller.archive.readers import CArchiveReader; from pathlib import Path
> for p in [Path('dist/variants/spotlight-safe.exe'),Path('dist/variants/spotlight-minimal.exe')]:
>  a=CArchiveReader(str(p)); rows=[(v[1],v[2],v[4],k) for k,v in a.toc.items()]; print('\n',p.name,len(rows)); [print(f'{c/1048576:6.2f} {n}') for c,u,t,n in sorted(rows,reverse=True)[:18]]"; Write-Output '--- MINIMAL NETWORK/CRYPTO ---'; & 'C:\Users\Moel\AppData\Local\Python\pythoncore-3.14-64\python.exe' -c "from PyInstaller.archive.readers import CArchiveReader; a=CArchiveReader('dist/variants/spotlight-minimal.exe'); [print(k) for k in a.toc if any(x in k.lower() for x in ('network','crypto','ssl','qml','quick','pdf','opengl'))]"`
>
> </details>
</details>

네. 현재 45MB 실행파일에는 사용하지 않는 Qt 구성요소가 상당수 포함돼 있습니다.

실제 비교 결과:

| 빌드 | 크기 | 감소율 | Smoke test |
|---|---:|---:|---|
| baseline | 44.84MB | — | 통과 |
| safe | 34.22MB | 24% | 통과 |
| minimal | 24.40MB | 46% | 통과 |

baseline에 포함된 주요 불필요 후보는 다음과 같습니다.

- `opengl32sw.dll`: 7.29MB
- `Qt6Quick.dll`: 2.78MB
- `Qt6Pdf.dll`: 2.35MB
- `Qt6Qml.dll`: 2.05MB
- Qt Virtual Keyboard, QML Models, 불필요한 이미지·플랫폼 플러그인
- QtNetwork/OpenSSL 관련 파일 약 3.6MB

Spotlight 소스는 `QtCore`, `QtGui`, `QtWidgets`만 사용합니다. 따라서 QML, Quick, PDF, Virtual Keyboard, 소프트웨어 OpenGL은 불필요합니다.

현재 [spotlight-minimal.exe](./dist/variants/spotlight-minimal.exe)가 가장 유력한 배포 후보입니다.

- 크기 24.4MB
- 실행 및 smoke report 정상
- Raw Input 시작 정상
- pynput 위험 문자열 0건
- 필수 Windows 플랫폼 플러그인 `qwindows.dll` 유지

다만 실제 배포 전에는 minimal 빌드로 다음 기능을 다시 확인해야 합니다.

- 설정 창과 색상 선택기
- 트레이 아이콘과 메뉴
- 오버레이 렌더링
- 고DPI·다중 모니터
- PowerPoint 슬라이드쇼
- GPU가 없거나 원격 세션인 환경

추가로 `minimal`에도 네트워크를 사용하지 않는데 `_ssl.pyd`, `libssl-3.dll`, `libcrypto-3.dll` 약 2.5MB가 남아 있습니다. 의존성 분석 후 이들까지 제외하면 약 21~22MB까지 줄일 여지가 있습니다.

보안 측면에서도 minimal이 낫습니다. QML, PDF, 네트워크/TLS, 이미지 코덱처럼 앱이 사용하지 않는 파서와 런타임을 제거하면 검토 대상과 잠재 공격 표면이 함께 줄어듭니다. 다만 PySide6 자체 때문에 20MB대는 현실적인 하한에 가깝습니다.
