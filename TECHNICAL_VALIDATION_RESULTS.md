# Spotlight 기술 검증 결과

## 1. 문서 사용 방법

이 문서는 `TECHNICAL_VALIDATION_PLAN.md`에 따라 수행한 검증 결과를 기록한다. 검증하지 않은 항목은 추측으로 채우지 않고 `미검증` 상태로 둔다.

결과 상태는 다음 중 하나로 표시한다.

- `미검증`: 아직 검증하지 않음
- `진행 중`: 검증을 시작했으나 결론이 나지 않음
- `통과`: 통과 기준을 충족함
- `조건부 통과`: 사용할 수 있지만 제한 사항이 있음
- `실패`: 요구사항을 충족하지 못함
- `보류`: 외부 환경이나 추가 결정이 필요함

## 2. 검증 환경

| 항목 | 값 |
| --- | --- |
| 검증 날짜 | 2026-08-11 |
| Windows 버전 및 빌드 | Windows 11 24H2, 빌드 26100.8039 |
| Python 버전 | 3.11.2, AMD64 |
| PySide6 버전 | 6.11.1 |
| 입력 감지 라이브러리 | pynput 설치본 |
| 모니터 구성 | HDMI SWITCH 단일 모니터 |
| 해상도 및 DPI 배율 | 1920×1080, 96 DPI, 배율 100% |
| Windows 커서 스타일 | Windows 기본 Aero 커서 |
| Windows 포인터 크기 | 기본 크기, `CursorBaseSize` 미지정 또는 32 |
| 실행 권한 | 일반 사용자 권한, GUI 실행만 샌드박스 외부 승인 |

## 3. 결과 요약

| ID | 검증 항목 | 상태 | 결론 또는 주요 제한 사항 |
| --- | --- | --- | --- |
| P-01 | 현재 포인터 크기 읽기 | 조건부 통과 | `CursorBaseSize`를 읽을 수 있으나 기본값일 때 항목이 없을 수 있어 32로 해석해야 함 |
| P-02 | 포인터 크기 1~15 적용 | 통과 | 레지스트리 방식은 실패했으나 `CopyImage`와 `SetSystemCursor` 방식으로 32×32→48×48 적용 성공 |
| P-03 | 주요 커서 모양 일괄 반영 | 통과 | `.cur`, `.ani`, 시스템 리소스를 포함한 14개 커서 모두 48×48 복제·적용 성공 |
| P-04 | 정상 종료 시 원래 크기 복원 | 통과 | 값과 값의 존재 여부를 복원했고 복구 파일이 제거됨 |
| P-05 | 비정상 종료 후 복구 | 조건부 통과 | 복구 파일을 이용한 수동·다음 실행 복구 성공, 강제 종료 자체는 미시험 |
| P-06 | 사용자 직접 변경 보호 | 보류 | 비교 규칙은 설계했으나 동시 변경 시험은 미실시 |
| O-01 | 투명 강조 원 표시 | 통과 | PySide6 투명 최상위 오버레이 표시 및 데스크톱 캡처 픽셀 확인 |
| O-02 | 마우스 입력 통과 | 통과 | 보정 후 좌우 클릭 2회와 휠 2회가 아래 테스트 창에 전달됨 |
| O-03 | 다중 모니터 및 DPI | 보류 | 단일 모니터 100% 환경만 통과 |
| O-04 | 오버레이 성능 | 조건부 통과 | 오프스크린 링 600프레임을 약 0.168초에 렌더링, 실제 장시간 부하는 미측정 |
| I-01 | 전역 클릭 감지 | 통과 | 왼쪽·오른쪽 누름 이벤트 감지 |
| I-02 | 전역 휠 감지 | 통과 | 위 `+1`, 아래 `-1` 감지 |
| I-03 | 관리자 권한 프로그램 위 감지 | 보류 | 관리자 권한 대상 프로그램으로 시험하지 않음 |
| A-01 | 클릭 애니메이션 | 조건부 통과 | 완전 불투명 프레임 생성 및 저장, 연속 클릭 시각 평가는 미실시 |
| A-02 | 휠 방향별 애니메이션 | 통과 | 바깥 방향 반지름 증가와 안쪽 방향 반지름 감소 확인 |
| R-01 | 디스플레이 캡처 | 조건부 통과 | Qt 데스크톱 캡처에는 포함됨. OBS는 현재 환경에서 미검증 |
| R-02 | 창 캡처 | 보류 | 별도 오버레이 창 특성상 실제 프로그램별 확인 필요 |
| R-03 | 화면 공유 | 보류 | 화상회의 프로그램으로 시험하지 않음 |

## 4. 상세 결과

### P-01~P-06 포인터 크기와 복구

- 사용한 스크립트: `validation/cursor_size_probe.py`
- 시작 상태: 기본 크기 1, `CursorBaseSize`가 없거나 DWORD 32
- 시험 설정: 크기 2에 대응하는 DWORD 48을 1초간 적용
- 레지스트리 변경: 32→48→32 또는 값 없음 상태로 복구 성공
- `SystemParametersInfoW(SPI_SETCURSORS)` 반환값: 성공
- 시스템 커서 메트릭: 적용 전·중·후 모두 32×32
- 활성 커서 비트맵: 적용 전·중·후 모두 32×32, 핫스폿 `(0, 0)`
- 복구 파일을 이용해 오류 후 DWORD 32 복구에 성공
- 최종 확인 시 기본값과 동일한 `CursorBaseSize` 미지정 상태이며 복구 파일은 없음

결론: `CursorBaseSize`를 직접 쓰고 시스템 커서를 다시 불러오는 것만으로는 Windows 설정 앱의 포인터 크기 변경을 재현할 수 없다. 설정 앱은 추가로 크기별 커서 파일을 생성하거나 내부 접근성 처리 과정을 수행하는 것으로 판단된다. 이 레지스트리 방식은 제품 구현에 그대로 채택하지 않는다.

#### 추가 검증: 시스템 커서 임시 교체

- 사용한 스크립트: `validation/system_cursor_probe.py`, `validation/all_system_cursors_probe.py`
- 사용 API: `CopyImage`, `SetSystemCursor`, `SystemParametersInfo(SPI_SETCURSORS)`
- 단일 화살표 커서: 32×32→48×48 변경 성공
- 화살표 커서 복원: 48×48→32×32, 크기와 핫스폿 일치
- 전체 대상: 시스템 커서 14종
- 48×48 복제 성공: 14종
- 실제 복제 비트맵 크기 일치: 14종
- 시스템 적용 성공: 14종
- `.ani` 대기 커서: `Wait`, `AppStarting` 모두 성공
- 현재 커서 테마의 파일 경로를 우선 사용하고 빈 항목은 시스템 리소스로 대체
- 전체 적용 후 저장된 Windows 커서 구성 복원 성공

결론: Windows 사용자 설정과 레지스트리를 직접 변경하지 않고 Spotlight 실행 중에만 현재 커서 테마를 확대할 수 있다. 제품 구현은 이 방식을 채택한다.

### O-01~O-04 투명 오버레이

- 사용한 스크립트: `validation/run_validation.py`, `validation/animation_probe.py`
- `WA_TranslucentBackground`: 적용 성공
- `WA_TransparentForMouseEvents`: 적용 성공
- `WindowTransparentForInput`: 적용 성공
- `WindowStaysOnTopHint`: 적용 성공
- `Qt.Tool`: 적용 성공
- 데스크톱 캡처 저장: 성공
- 캡처 내 강조 원 색상 픽셀: 1,348개 이상 확인
- 아래 테스트 창에 전달된 입력: 클릭 2회, 휠 2회

첫 번째 입력 통과 실행은 성공했고, 두 번째 실행에서는 테스트 창이 활성화되지 않아 실패했다. 테스트 창을 명시적으로 활성화한 최종 실행은 다시 성공했다. 제품에서는 오버레이가 포커스를 취하지 않도록 하고 창 활성화와 무관하게 입력 통과 속성을 유지해야 한다.

### I-01~I-03 전역 입력

- 사용한 스크립트: `validation/run_validation.py`
- 왼쪽 클릭 감지: 성공
- 오른쪽 클릭 감지: 성공
- 휠 위 방향 `+1` 감지: 성공
- 휠 아래 방향 `-1` 감지: 성공
- 원래 입력 전달: 성공
- 관리자 권한 프로그램 위 입력: 미검증

최종 실행은 모든 결과를 기록했지만 입력 리스너 스레드가 즉시 끝나지 않아 외부 명령 제한시간에 종료됐다. 제품에서는 리스너를 애플리케이션 수명주기에 연결하고 종료 시 명시적으로 중지·대기해야 한다.

### A-01~A-02 애니메이션

- 사용한 스크립트: `validation/animation_probe.py`
- 바깥 방향 반지름: 25, 36.25, 47.5, 58.75, 70
- 안쪽 방향 반지름: 70, 58.75, 47.5, 36.25, 25
- 클릭 및 휠 색상 알파: 255로 완전 불투명
- 320×320 투명 이미지 600프레임 렌더링: 약 0.168초
- 환산 렌더링 처리량: 약 3,568프레임/초

수학적 방향, 불투명도와 기본 렌더링 성능은 통과했다. 실제 연속 입력 시 효과 중첩 정책과 시각적 자연스러움은 제품 프로토타입에서 추가 확인한다.

### R-01~R-03 캡처 호환성

- Qt의 전체 데스크톱 캡처에 별도 투명 오버레이가 포함됨
- 결과 이미지: `validation/artifacts/overlay_capture.png`
- 현재 환경에서 OBS 실행 파일을 확인하지 못해 OBS 디스플레이·창 캡처는 미검증
- Windows 캡처 도구는 존재하지만 자동 녹화 결과 분석은 미실시
- 화상회의 화면 공유는 미검증

결론: 디스플레이 합성 결과에는 오버레이가 포함되지만, 특정 창만 캡처하는 방식은 별도 오버레이 창이 제외될 가능성이 있어 지원을 확정할 수 없다.

## 5. 포인터 설정 복구 기록

포인터 크기 변경 검증을 수행할 때만 작성한다.

| 항목 | 값 |
| --- | --- |
| 변경 전 포인터 크기 | 기본값, 값 없음 또는 32 |
| Spotlight 적용 크기 | 시험값 48, 크기 단계 2 |
| 종료 시 현재 크기 | 48 |
| 복원한 크기 | 기본값, 값 없음 또는 32 |
| 커서 스타일 보존 여부 | 레지스트리의 커서 경로는 변경하지 않음 |
| 복구 파일 위치 | `validation/artifacts/cursor_recovery.json` |
| 복구 성공 여부 | 성공, 현재 복구 파일 없음 |

## 6. 화면 녹화 호환성 결과

| 프로그램 | 캡처 방식 | 포인터 크기 | 강조 원 | 클릭 효과 | 휠 효과 | 상태 및 비고 |
| --- | --- | --- | --- | --- | --- | --- |
| Qt 데스크톱 캡처 | 전체 화면 | 미검증 | 포함 | 미검증 | 미검증 | 강조 원 색상 픽셀 확인 |
| Windows 캡처 도구 | 화면 녹화 | 미검증 | 미검증 | 미검증 | 미검증 | 프로그램 존재 확인, 실제 녹화 미실시 |
| OBS | 디스플레이 캡처 | 미검증 | 미검증 | 미검증 | 미검증 | 현재 환경에서 OBS 미확인 |
| OBS | 창 캡처 | 미검증 | 미검증 | 미검증 | 미검증 | 현재 환경에서 OBS 미확인 |
| PowerPoint | 전체 화면 발표 | 미검증 | 미검증 | 미검증 | 미검증 |  |
| 화상회의 프로그램 | 전체 화면 공유 | 미검증 | 미검증 | 미검증 | 미검증 |  |
| 화상회의 프로그램 | 특정 창 공유 | 미검증 | 미검증 | 미검증 | 미검증 |  |

## 7. 최종 기술 결정

모든 핵심 검증이 끝난 후 작성한다.

| 결정 항목 | 채택 방식 | 근거 | 남은 제한 사항 |
| --- | --- | --- | --- |
| 포인터 크기 변경 | 현재 테마를 `CopyImage`로 확대 후 `SetSystemCursor`로 임시 적용 | 14개 커서의 48×48 복제·적용 성공 | Windows 10 및 사용자 지정 테마 추가 시험 필요 |
| 포인터 설정 복원 | 원래 값과 존재 여부를 복구 파일에 저장 | 실제 오류 후 복구 성공 | 사용자 동시 변경 보호 추가 필요 |
| 비정상 종료 복구 | 다음 실행 시 복구 파일 우선 처리 | 복구 파일 기반 수동 복구 성공 | 강제 종료 시험 필요 |
| 투명 오버레이 | PySide6 투명 `Qt.Tool` 최상위 창 | 표시·입력 통과·데스크톱 캡처 통과 | 다중 모니터·혼합 DPI 미검증 |
| 전역 입력 감지 | pynput 프로토타입 사용 가능 | 좌우 클릭과 양방향 휠 감지 성공 | 종료 수명주기와 관리자 앱 미검증 |
| 클릭 애니메이션 | QPainter 불투명 링 | 프레임 렌더링 성공 | 연속 입력 중첩 정책 미정 |
| 휠 애니메이션 | 반지름 선형 보간 | 양방향 수치 검증 성공 | 최종 이징과 지속 시간 미정 |
| 화면 녹화 지원 범위 | 디스플레이 합성 캡처 우선 | Qt 데스크톱 캡처 포함 확인 | OBS·창 캡처·화상회의 미검증 |

## 8. 미해결 문제

| ID | 문제 | 영향 | 필요한 조치 | 상태 |
| --- | --- | --- | --- | --- |
| V-01 | Windows 설정 앱의 크기 값을 직접 바꾸는 공식 API는 찾지 못함 | 레지스트리 직접 변경 방식 사용 불가 | 공식 `CopyImage`·`SetSystemCursor` 임시 교체 방식으로 대체 | 해결 |
| V-02 | 다중 모니터와 혼합 DPI 환경이 없음 | 좌표 및 크기 보정 미확인 | 해당 환경에서 후속 시험 | 보류 |
| V-03 | 관리자 권한 대상 앱 입력 감지 미시험 | 일부 앱 위에서 효과가 발생하지 않을 수 있음 | 상승된 테스트 앱으로 후속 시험 | 보류 |
| V-04 | OBS와 화상회의 프로그램 미확인 | 실제 배포 지원 범위 미확정 | 프로그램 설치 환경에서 수동 녹화·공유 시험 | 보류 |
| V-05 | pynput 리스너가 결과 기록 후 즉시 종료되지 않은 실행이 있음 | 종료 지연 가능성 | 제품에서는 명시적인 훅 해제와 스레드 종료 검증 | 진행 중 |

## 9. 2단계 기본 프로그램 검증

### 구현 범위

- `%LOCALAPPDATA%\Spotlight\settings.ini` 최초 자동 생성 및 시작 시 로딩
- 포인터 크기 1~15 검증과 픽셀 크기 변환
- 현재 커서 테마의 시스템 커서 14개 확대 적용
- 적용 전 복구 표식 생성 및 종료 시 원래 커서 복원
- 60FPS 주기의 마우스 위치 추적
- 투명·입력 통과·항상 위 강조 원 오버레이
- 작업표시줄을 사용하지 않는 시스템 트레이 아이콘
- 트레이의 프로그램 종료 메뉴

### 자동 검증 결과

| 검증 항목 | 결과 | 세부 내용 |
| --- | --- | --- |
| Python 구문 검사 | 통과 | `src`, `tests`, `run_spotlight.py` 컴파일 성공 |
| 설정 단위 테스트 | 통과 | 기본값, 잘못된 값 대체, 사용자 값 로딩 |
| 포인터 단계 단위 테스트 | 통과 | 1→32px, 2→48px, 15→256px 및 범위 오류 |
| 단위 테스트 합계 | 통과 | 5개 테스트 모두 성공 |
| 기본 설정 스모크 테스트 | 통과 | 크기 1, 커서 14개, 오버레이, 트레이 확인 |
| 확대 설정 스모크 테스트 | 통과 | 크기 2, 목표 48px, 커서 14개 적용 |
| 오버레이 영역 | 통과 | 1920×1080 전체 화면 |
| 오버레이 입력 통과 속성 | 통과 | 활성화 확인 |
| 시스템 트레이 | 통과 | 사용 가능 및 아이콘 표시 확인 |
| 정상 종료 복원 | 통과 | 두 차례 모두 원래 커서 복원 |
| 복구 파일 정리 | 통과 | 종료 후 복구 파일 없음 |

스모크 테스트 보고서는 `validation/artifacts/stage2_smoke.json`과 `validation/artifacts/stage2_smoke_size2.json`에 저장했다.

## 10. 트레이 설정 메뉴 검증

### 구현 범위

- 트레이 메뉴에 `설정` 항목 추가
- 포인터, 강조 원, 클릭, 휠 탭으로 구성된 설정 창
- `적용`, `확인`, `취소` 동작
- 색상 선택기와 `#RRGGBB` 직접 입력
- 포인터 크기와 강조 원 설정의 실행 중 즉시 반영
- 클릭·휠 설정 저장 준비
- UTF-8 INI 파일 원자적 저장

### 자동 검증 결과

| 검증 항목 | 결과 | 세부 내용 |
| --- | --- | --- |
| 설정 저장 왕복 | 통과 | 저장 후 다시 읽은 설정 객체가 원본과 일치 |
| 전체 단위 테스트 | 통과 | 6개 테스트 성공 |
| 설정 창 생성 | 통과 | 트레이 애플리케이션 실행 중 설정 창 표시 |
| 포인터 설정 적용 | 통과 | 크기 1→2, 목표 크기 48px, 커서 14개 재적용 |
| 강조 원 설정 적용 | 통과 | 지름 120→180px 즉시 반영 |
| INI 저장 | 통과 | `size=2`, `diameter=180` 확인 |
| 정상 종료 복원 | 통과 | 원래 시스템 커서 복원 |
| 복구 파일 정리 | 통과 | 종료 후 복구 파일 없음 |

GUI 결과는 `validation/artifacts/settings_menu_smoke.json`에 기록했다.

## 11. 3단계 입력 효과 검증

### 구현 범위

- 일반 권한 전역 마우스 입력 훅
- 왼쪽·오른쪽 클릭 구분
- 클릭 위치에 고정되는 불투명 원 효과
- 휠 위 방향의 안쪽→바깥쪽 애니메이션
- 휠 아래 방향의 바깥쪽→안쪽 애니메이션
- 클릭 180ms, 휠 250ms 기본 지속 시간
- 최근 효과 최대 3개 유지
- 종료 시 입력 훅 명시적 해제

### 자동 검증 결과

| 검증 항목 | 결과 | 세부 내용 |
| --- | --- | --- |
| 전체 단위 테스트 | 통과 | 설정·포인터·애니메이션 9개 테스트 성공 |
| 좌클릭 감지 | 통과 | 합성 좌클릭 4회 감지 |
| 우클릭 감지 | 통과 | 합성 우클릭 1회 감지 |
| 휠 위 감지 | 통과 | `wheel-out` 감지 |
| 휠 아래 감지 | 통과 | `wheel-in` 감지 |
| 최대 효과 제한 | 통과 | 최대 활성 효과 3개 |
| 입력 통과 | 통과 | 아래 테스트 창이 클릭 5회·휠 2회 수신 |
| 커서 확대 병행 | 통과 | 활성 커서 48×48 확인 |
| 애니메이션 렌더링 | 통과 | 오버레이 표면에서 강조 원과 효과 3개 확인 |
| 입력 훅 종료 | 통과 | 리스너 스레드 정상 종료 |
| 커서 복원 | 통과 | 종료 후 원래 시스템 커서 복원 |
| 복구 파일 정리 | 통과 | 종료 후 복구 파일 없음 |

결과 데이터는 `validation/artifacts/stage3_probe.json`, 렌더링 결과는 `validation/artifacts/stage3_overlay_surface.png`에 저장했다.
